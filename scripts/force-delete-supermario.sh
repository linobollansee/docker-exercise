#!/bin/bash

# Force Remove Super Mario Docker Container - Bash Script
# This script forcefully removes the Super Mario container
# Container will be stopped (if running) and removed in one command

docker rm -f supermario
